---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: ksanstorages.ksan.openshift.io
spec:
  group: ksan.openshift.io
  names:
    kind: KSANStorage
    listKind: KSANStorageList
    plural: ksanstorages
    singular: ksanstorage
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: KSANStorage is the Schema for the ksanstorages API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KSANStorageSpec defines the desired state of KSANStorage
            properties:
              devicesConfig:
                properties:
                  devices:
                    items:
                      type: string
                    minItems: 1
                    type: array
                    uniqueItems: true
                  mode:
                    enum:
                    - AllDevices;SelectedDevices
                    type: string
                required:
                - devices
                - mode
                type: object
              kubesanParams:
                additionalProperties:
                  type: string
                type: object
              name:
                default: KsanCluster
                type: string
            required:
            - devicesConfig
            - name
            type: object
          status:
            description: KSANStorageStatus defines the observed state of KSANStorage
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
